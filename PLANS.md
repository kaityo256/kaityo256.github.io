# PLANS.md

## 目的

`kaityo256.github.io` リポジトリの `_posts` ディレクトリ内にある記事を調査し、本文中に残っている以下の外部記事リンクを、このブログ内の対応する記事へのリンクへ置き換える。

- `qiita.com/kaityo256`
- `zenn.dev/kaityo256`

ブログの公開URL:

- `https://kaityo256.github.io/`

作業は必ず次の2段階に分ける。

1. 変更候補を調査し、リンク先の変更一覧を提出する
2. ユーザーの明示的な承認を得た後に、ファイルを書き換える

**承認前に `_posts` 内のファイルを変更してはならない。**

## 対象範囲

調査対象:

- `_posts` 以下にある記事ファイル
- 記事本文、Markdownリンク、HTMLリンクなどに含まれるURL

置換候補となるリンク:

- `https://qiita.com/kaityo256/...`
- `http://qiita.com/kaityo256/...`
- `https://zenn.dev/kaityo256/...`
- `http://zenn.dev/kaityo256/...`
- URL末尾にクエリ文字列、フラグメント、末尾スラッシュが付いたもの
- Markdown、HTML、プレーンテキストなど、記法の違いを含むもの

原則として対象外:

- `_posts` 以外のファイル
- `qiita.com/kaityo256` または `zenn.dev/kaityo256` 以外の外部リンク
- コード例や引用として意図的に記載されており、記事への参照ではないURL
- 対応するブログ記事を十分な確度で特定できないリンク

対象外と判断したリンクも、判断理由とともに調査結果へ記載する。

## 対応するブログ記事の特定方法

各外部リンクについて、リンク先の記事と同じ内容を持つ `_posts` 内の記事を探す。

判定には、可能な範囲で次の情報を使用する。

1. 外部記事のURLスラッグまたは記事ID
2. `_posts` のファイル名
3. Front Matterの `title`
4. Front Matterの `date`
5. Front Matterに記載された元記事URL、canonical URL、slug、識別子
6. 記事本文の見出し、冒頭、主要な内容
7. QiitaまたはZennの記事タイトル
8. 公開日や更新日
9. コード例、図、章構成などの一致

タイトルが似ているだけでは確定せず、本文やメタデータも確認する。

複数の記事が候補になる場合や、対応関係に確信を持てない場合は、自動的に置換対象へ含めない。「要確認」として候補と根拠を報告する。

## ブログ内URLの生成

対応する `_posts` の記事について、Jekyllの設定とFront Matterを確認し、実際に公開されるURLを求める。

確認対象には以下を含める。

- `_config.yml` の `permalink` 設定
- 各記事のFront Matterにある `permalink`
- ファイル名の日付とslug
- `baseurl`
- `url`
- カテゴリ設定
- その他、生成URLへ影響する設定

リンク先は、実際に公開される記事URLと一致させる。

基本となる公開URL:

`https://kaityo256.github.io/`

リポジトリ内の既存記事で内部リンクの書き方が統一されている場合は、その形式を優先する。たとえば以下の形式が考えられる。

- 絶対URL: `https://kaityo256.github.io/...`
- ルート相対URL: `/...`
- Liquidを使ったURL: `{{ site.baseurl }}...`
- `{% post_url ... %}`

ただし、置換案の一覧には、最終的に生成される公開URLも併記する。

## フェーズ1: 調査と変更一覧の提出

### 1. リポジトリ設定の確認

最初に以下を確認する。

- `_config.yml`
- `_posts` のファイル構成
- Front Matterの一般的な形式
- 既存の内部リンクの記法
- Jekyllのpermalinkルール

この時点ではファイルを変更しない。

### 2. 外部リンクの抽出

`_posts` 以下を検索し、`qiita.com/kaityo256` または `zenn.dev/kaityo256` を含む箇所をすべて抽出する。

各リンクについて以下を記録する。

- リンクが存在するファイル
- 行番号
- 現在のURL
- リンクテキストまたは周辺の文脈
- Markdown、HTMLなどの記法
- 同じURLが複数回登場する場合の出現箇所

検索結果が0件の場合も、その旨を報告する。

### 3. 対応記事の探索

抽出した外部リンクごとに、対応する `_posts` 内の記事を探す。

必要に応じて外部ページの情報を確認してよい。ただし、外部ページへアクセスできない場合は、URL、リポジトリ内のメタデータ、記事本文など、利用可能な情報だけで調査する。

対応関係を次のいずれかに分類する。

- `確定`: 同一記事であることを十分に確認できた
- `要確認`: 候補はあるが、一意に確定できない
- `対応なし`: `_posts` 内に対応する記事が見つからない
- `置換不要`: コード例、引用、説明用URLなどであり、内部記事へのリンクへ変更すべきでない

### 4. 変更一覧の提出

ファイルを編集する前に、Markdown形式で変更一覧を提示する。

最低限、次の列を含める。

| No. | 参照元ファイル | 行番号 | 現在のURL | 対応する記事ファイル | 変更後の公開URL | 判定 | 根拠 |
| --: | -------------- | -----: | --------- | -------------------- | --------------- | ---- | ---- |

必要に応じて、変更前後の記述もコードブロックまたはdiff形式で示す。

例:

```diff
-[記事タイトル](https://zenn.dev/kaityo256/articles/example)
+[記事タイトル](/2024/01/01/example.html)
```

一覧の後に、次の集計を付ける。

- 検出したリンク数
- 変更を提案するリンク数
- `要確認` のリンク数
- `対応なし` のリンク数
- `置換不要` のリンク数
- 変更対象となるファイル数

同じ外部URLが複数箇所に存在する場合は、すべての変更箇所を明示する。

### 5. 承認待ち

変更一覧を提出したら作業を停止し、ユーザーの承認を待つ。

次のような明示的な承認があるまで編集してはならない。

- 「承認します」
- 「この一覧で書き換えてください」
- 「要確認を除いて実行してください」
- 個別の変更項目を指定した実行指示

承認内容に条件や除外指定がある場合は、その範囲だけを変更する。

変更一覧を提出したメッセージ内で、続けて書き換えを実行してはならない。

## フェーズ2: 承認後の書き換え

### 1. 承認範囲の確認

ユーザーが承認した項目だけを対象にする。

- 承認されていない項目は変更しない
- `要確認` の項目は、明示的に承認された場合のみ変更する
- 承認後に新しい候補が見つかった場合は、勝手に追加変更せず、別途報告する

### 2. 最小限の編集

承認されたURLだけを置換する。

以下は変更しない。

- 記事本文の表現
- リンクテキスト
- Front Matter
- 改行
- インデント
- Markdownの記法
- 関係のないリンク
- 関係のない空白やフォーマット

ただし、リンク先を正しく変更するために記法の調整が必要な場合は、理由を報告したうえで最小限の変更を行う。

### 3. 変更後の検証

編集後、以下を確認する。

1. 承認された外部URLが意図どおり置換されている
2. 承認されていないURLが変更されていない
3. MarkdownまたはHTMLのリンク構文が壊れていない
4. 変更後のURLが対応する記事の公開URLと一致している
5. `_posts` 内に対象URLが残っている場合、その箇所と理由が説明できる
6. 利用可能であればJekyllのビルドまたは既存のテストを実行する
7. ビルドを実行できない場合は、その理由を報告する

Jekyllのビルドを実行する場合は、リポジトリで指定されている方法を優先する。例:

```bash
bundle exec jekyll build
```

依存関係のインストールや大規模な環境変更が必要な場合は、勝手に実行せず、既存環境で可能な検証まで行う。

### 4. 完了報告

変更後、以下を報告する。

- 変更したファイル一覧
- 置換したリンク数
- 各リンクの変更前と変更後
- 変更しなかった候補と理由
- 実施した検証
- ビルドまたはテストの結果
- 残っている `qiita.com/kaityo256` および `zenn.dev/kaityo256` のリンクと、その理由

可能であれば、最終的な差分の要約も提示する。

## 禁止事項

- 変更一覧の承認前にファイルを書き換えること
- 対応記事を推測だけで決定すること
- `要確認` の項目を無断で変更すること
- 対象外の外部リンクを変更すること
- 記事本文を不要に整形すること
- リンクテキストを無断で変更すること
- Front Matterを無断で変更すること
- 調査と書き換えを同じ承認前フェーズで実施すること
- 新規記事を作成して対応関係を補うこと
- 対応する記事が見つからないリンクをトップページへ置き換えること
- リダイレクトされるという理由だけで、対応関係を確認せず置換すること

## 完了条件

以下をすべて満たした時点で作業完了とする。

- `_posts` 内の対象リンクをすべて調査した
- 承認前に変更一覧を提出した
- ユーザーが承認した項目だけを書き換えた
- 変更後のリンク先を検証した
- 対象URLが残っている場合は、各リンクについて理由を報告した
- 実施した変更と検証結果を最終報告した

## 実施記録

### 2026-08-06 調査結果

`_posts` 内の `qiita.com/kaityo256` および `zenn.dev/kaityo256` を含むリンクを調査した。

- 検出したリンク出現数: 553
- 変更提案リンク数: 549
- `要確認`: 3
- `対応なし`: 2
- `置換不要`: 0
- 変更対象ファイル数: 162

公開URLは `_config.yml` の `url: https://kaityo256.github.io`、`baseurl: ""`、各記事の Front Matter `permalink` から、原則として `https://kaityo256.github.io/<permalink>` と判断した。

### 承認内容

ユーザー承認により、以下を実施した。

1. `要確認` のうち、Qiita固有アンカー `#_reference...` が付いた2 URLについて、内部アンカーなしで対応記事本体へリンクする。
2. 残りの `確定` 548件をブログ内URLへ置換する。

### 書き換え結果

以下のコミットで、承認済みの置換を実施した。

- Commit: `e0c4771 Replace external article links with blog URLs`
- 変更ファイル数: 162
- 差分: 543 insertions, 543 deletions
- 置換済みリンク数: 551
  - `要確認` 承認分: 3出現
  - `確定` 承認分: 548出現

Qiita見出しリンク `https://qiita.com/kaityo256/items/2977d53e70bbffd4d601#%E3%83%86%E3%82%AD%E3%82%B9%E3%83%88%E3%81%AE%E6%8E%A2%E3%81%97%E6%96%B9` は、対応記事の見出しへ `https://kaityo256.github.io/ppt_grep#テキストの探し方` として置換した。

### 残っていた対象リンクの解消

承認済み作業後も残っていた対象リンク2出現は、ユーザー確認により `_posts/2017-01-20-minimal_vertexcover.md` が対応記事であると判明したため、追加対応で解消済み。

| No. | 参照元ファイル | 行番号 | 変更後URL | 判定 |
| --: | -------------- | -----: | --------- | ---- |
| 1 | `_posts/2017-01-26-minimal_hittingset.md` | 12 | `https://kaityo256.github.io/minimal_vertexcover` | 解消済み |
| 2 | `_posts/2017-01-26-minimal_hittingset.md` | 22 | `https://kaityo256.github.io/minimal_vertexcover` | 解消済み |

### 検証結果

実施した確認:

- 承認済みの外部記事リンクが置換済みであることを確認した。
- 残存する対象リンクが上記2出現のみであることを確認した。
- `git diff --check` を実行し、置換対象行に既存由来の末尾空白が残っていることを確認した。本文・空白の不要整形を避ける方針のため未修正。
- `bundle exec jekyll build` を実行したが、`assets/main.scss` の Sass embedded 変換で `end of file reached` となり失敗した。Markdownリンク置換とは別系統の既存ビルド環境問題と判断した。

### 今後の要確認事項

残存していた `対応なし` 2出現は、2026-08-06 の追加対応で解消済み。

### 2026-08-06 追加対応: 極小ヒッティングセット記事の対応先修正

残っていた対象リンク No.1, No.2 について、ユーザー確認により対応記事は `_posts/2017-01-20-minimal_vertexcover.md` と判明した。

- `_posts/2017-01-26-minimal_hittingset.md` 内の2出現を `https://kaityo256.github.io/minimal_vertexcover` へ置換した。
- 内部アンカーは付与していない。

### 2026-08-06 追加対応: ParaView記事の取り違え修正

`_posts/2017-07-26-paraview_vtk1.md` は実際にはQiitaの「ParaViewでVTKレガシーフォーマットを使う その2」 (`https://qiita.com/kaityo256/items/851c559a8f4c43de329e`) に対応する記事だったため、以下の修正を行った。

- `_posts/2017-07-26-paraview_vtk1.md` を `_posts/2017-07-26-paraview_vtk2.md` へリネームした。
- Front Matterの `title` と `permalink` を `その2` / `paraview_vtk2` に修正した。
- その2本文内の画像パスを `/assets/images/paraview_vtk2/...` に修正した。
- `assets/images/paraview_vtk1` を `assets/images/paraview_vtk2` へリネームした。
- その2冒頭の「前回の記事」リンクを `https://kaityo256.github.io/paraview_vtk1` に修正した。
- Qiitaの「ParaViewでVTKレガシーフォーマットを使う その1」 (`https://qiita.com/kaityo256/items/661833e9e2bfbac31d4b`) を `_posts/2017-07-26-paraview_vtk1.md` として追加した。
- その1の画像6枚を `assets/images/paraview_vtk1/` に保存し、本文中の画像参照をブログ内パスに修正した。

この対応により、ParaView記事の残存対象リンク1出現は解消済み。
